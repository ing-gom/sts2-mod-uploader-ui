{
  "_comment": "이 파일을 config.json 으로 복사해 필요한 값만 채우세요. 모두 선택사항입니다.",

  "uploader_dir": "C:/Tools/ModUploader-win-x64",
  "game_path": "",
  "workspaces": "",
  "port": 8791,

  "_sources_comment": "Build+Upload(dotnet build -c Release) 를 쓰고 싶은 모드만 ModId -> csproj 경로로 매핑. 없으면 Upload(설치본 그대로)만 사용.",
  "sources": {
    "YourModId": "C:/path/to/YourMod/YourMod.csproj"
  },

  "exclude": []
}
